"""
Wi-Fi active module (skeleton).

Later will:
  - Perform targeted active operations (association, auth checks, etc.)
"""

def run(profile: str) -> None:
    print(f"[wifi_active] Running active Wi-Fi ops for profile={profile} (no-op stub)")
