"""
Bluetooth active module (skeleton).

Later will:
  - Perform targeted active operations (connect, service queries, etc.)
"""

def run(profile: str) -> None:
    print(f"[bt_active] Running BT active ops for profile={profile} (no-op stub)")
