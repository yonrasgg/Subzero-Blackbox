"""
Bluetooth passive recon module (skeleton).

Later will:
  - Scan for nearby BT/BLE devices
  - Classify by type / RSSI
"""

def run(profile: str) -> None:
    print(f"[bt_recon] Running BT recon for profile={profile} (no-op stub)")
