"""
Wi-Fi passive recon module (skeleton).

Later will:
  - Use monitor-mode interface
  - Capture beacons/probes
  - Store PCAP / JSON summaries under data/captures
"""

def run(profile: str) -> None:
    print(f"[wifi_recon] Running passive recon for profile={profile} (no-op stub)")
